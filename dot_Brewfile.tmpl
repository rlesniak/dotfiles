# chezmoi:template:left-delimiter="# {{" right-delimiter="}}"
# {{ $isDarwin := eq .chezmoi.os "darwin" -}}
# {{ $hostname := .chezmoi.hostname -}}

# Packages outside conditionals install on every host that uses this repo.
# Use OS conditionals for platform-specific packages and hostname conditionals
# only when a machine genuinely needs different tools.

tap "tw93/tap"

brew "bat"
brew "chezmoi"
brew "coreutils"
brew "ffmpeg"
brew "fish"
brew "fzf"
brew "gh"
brew "git"
brew "watchman"
brew "tw93/tap/mole"

# {{ if $isDarwin -}}
brew "mas"

cask "arc"d
cask "bitwarden"
cask "font-geist-mono"
cask "font-jetbrains-mono"
cask "font-jetbrains-mono-nerd-font"
cask "font-monaspace"
cask "font-source-code-pro"
cask "font-source-code-pro-for-powerline"
cask "ghostty"
cask "github"
cask "handy"
cask "orbstack"
cask "muxy"
cask "raycast"
cask "stats"
cask "tailscale-app"
cask "the-unarchiver"
cask "visual-studio-code"
cask "yaak"
cask "shottr"
cask "zed"
cask "zen"
cask "zulu@17"

mas "Xcode", id: 497799835
# {{ end -}}

# {{ if eq $hostname "work-mac" -}}
# Add host-specific packages here when a machine truly needs them.
# {{ end -}}
